const getState = ({ getStore, getActions, setStore }) => {
  return {
    store: {
      /* enterprise: "",
      descriptions: "",
      rol: "", */
    },

    actions: {
      /* handleChange: (e) => {
        setStore({
          [e.target.name]: e.target.value,
        });
      }, */
    },
  };
};
export default getState;
